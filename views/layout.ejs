<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link rel="stylesheet" href="/static/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        .offcanvas-body {
            padding: 1rem;
        }
        .sidebar-icon {
            font-size: 1.5rem;
            color: #333;
            text-decoration: none;
            display: flex;
            align-items: center;
            padding: 0.5rem;
            transition: color 0.3s;
        }
        .sidebar-icon i {
            margin-right: 10px;
        }
        .sidebar-icon span {
            font-family: 'Arial', sans-serif; /* Mude para a fonte desejada */
            font-size: 1rem; /* Ajuste o tamanho do texto se necessário */
        }
        .sidebar-icon:hover {
            color: #007bff;
        }
    </style>
</head>
<body>
    <!-- Botão para abrir a sidebar -->
    <button class="btn btn-dark m-2" type="button" data-bs-toggle="offcanvas" data-bs-target="#staticBackdrop" aria-controls="staticBackdrop">
      <i class="fa-solid fa-bars"></i>
    </button>
  
    <!-- Offcanvas Sidebar -->
    <div class="offcanvas offcanvas-start bg-dark" data-bs-backdrop="static" tabindex="-1" id="staticBackdrop" aria-labelledby="staticBackdropLabel">
        <div class="offcanvas-header">
            <h5 class="offcanvas-title text-white" id="staticBackdropLabel">Sidebar</h5>
            <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
        </div>
        <div class="offcanvas-body">
            <a href="#" class="sidebar-icon text-white">
                <i class="fas fa-home"></i><span>Home</span>
            </a>
            <a href="#" class="sidebar-icon text-white">
                <i class="fas fa-user"></i><span>Usuário</span>
            </a>
            <a href="#" class="sidebar-icon text-white">
                <i class="fas fa-cog"></i><span>Configurações</span>
            </a>
            <a href="#" class="sidebar-icon text-white">
                <i class="fas fa-info-circle"></i><span>Informações</span>
            </a>
        </div>
    </div>
  
    <%- body %>

    <script src="/static/js/bootstrap.bundle.min.js"></script>
</body>
</html>
